name: My-Custom-Ansible-CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run myplaybook on Shared-Runner
    #    uses: root/action-ansible-playbook@v3
        with:
          playbook: myplaybook.yml
          directory: ./
